- name: devops course information
  hosts: local
  connection: local
  vars:
    course_name: "devops with aws" #string
    duration: 120 #number
    topics: # list
    - linux
    - shell
    - ansible
    - aws
    live: true # boolean
    tools:
      ci: jenkins
      cloud: aws
      containeization: docker
  tasks:
  - name: print course information
    debug:
      msg: "hello i am learning {{ course_name}}, duration: {{ durstion }}, topics are: {{ topics }}, is it {{live}}, 
      tools covered are: {{ tools }} "
